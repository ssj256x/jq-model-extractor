[project]
name = "jresolve"
version = "0.1.0"
description = "A declarative library to extract and transform JSON"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "jq>=1.10.0",
    "pydantic>=2.12.5",
    "result>=0.17.0",
]
